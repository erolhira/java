package lambda;

/*
 * Because all methods are implicitly abstract, it is redundant (but allowed) to declare a method as abstract. 
 * Because all interface methods are abstract, you may not provide any method implementation, not even an empty set of braces.
 */
public abstract interface IAbstractSample {	
	public abstract void abstractMethod(); 
}
